public class VoidRenderer implements Renderer {
    /**
     * The VoidRenderer class implements the Renderer interface with an empty renderBoard method.
     * When instances of this class are used as renderers, they do not display any information
     * about the game board. It serves as a placeholder or a silent renderer for scenarios
     * where visual rendering is not required.
     */
    public void renderBoard(Board board) {
    }
}
